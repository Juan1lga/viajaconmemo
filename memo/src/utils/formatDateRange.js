const MONTHS_ES = ["enero","febrero","marzo","abril","mayo","junio","julio","agosto","septiembre","octubre","noviembre","diciembre"];export const formatDateRange = (startDate, endDate, options = { style: "short" }) => { if (!startDate || !endDate) return ""; try { const sd = new Date(startDate); const ed = new Date(endDate); if (Number.isNaN(sd.getTime()) || Number.isNaN(ed.getTime())) return ""; const sDay = sd.getDate(); const eDay = ed.getDate(); const sMon = sd.getMonth(); const eMon = ed.getMonth(); const sYear = sd.getFullYear(); const eYear = ed.getFullYear(); const sameMonth = sMon === eMon && sYear === eYear; const sep = options.style === "short" ? "–" : " al "; if (sameMonth) { const month = MONTHS_ES[sMon]; if (options.style === "short") { return `${sDay}${sep}${eDay} de ${month}`; } return `del ${sDay}${sep}${eDay} de ${month}`; } else { const sLabel = `${sDay} de ${MONTHS_ES[sMon]}`; const eLabel = `${eDay} de ${MONTHS_ES[eMon]}`; if (options.style === "short") { return `${sLabel}${sep}${eLabel}`; } return `del ${sLabel}${sep}${eLabel}`; } } catch { return ""; } };export const formatDateLabel = (date) => { if (!date) return ""; try { const d = new Date(date); if (Number.isNaN(d.getTime())) return ""; return `${d.getDate()} de ${MONTHS_ES[d.getMonth()]}`; } catch { return ""; } };